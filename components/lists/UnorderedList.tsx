import styled from 'styled-components';

/**
 * an unordered list used for displaying Jokes and their categories
 */
const UnorderedList = styled.ul`
    overflow-y: auto;
    list-style-type: none;
    padding: 0;
    flex-grow: 1;
`;

export default UnorderedList;
