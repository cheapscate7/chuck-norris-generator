import styled from 'styled-components';

/**
 * a <main> that displays everthing that isnt the nav bar
 */
const PageContent = styled.main`
    max-width: 73rem;
    margin: auto;
`;

export default PageContent;
